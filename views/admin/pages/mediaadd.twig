{% extends 'admin/main.twig' %}
{% block admincontent %} 

<div class="row">
    <div class="col-md-12">
        <div class="overview-wrap">
            <h2 class="title-1">Новое видео</h2>
            <a href="{{path_for('admin.media')}}">Назад к списку →</a>
        </div>
    </div>
</div>

<div class="row m-t-10">
    <div class="col-md-12">
        {% include 'include/flash.twig' %} 
    </div>
</div>

<div class="row m-t-10">
<div class="col-lg-12">
        <div class="card">
        
        <form action="{{path_for('admin.addmedia')}}" method="post">
            
            <div class="card-header">
                Форма <strong>добавления</strong>
            </div>

            <div class="card-body card-block">
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group ">
                            <label for="company" class=" form-control-label">Название видео</label>
                            <input type="text" 
                                placeholder="Введите название" 
                                class="form-control"
                                name="videoname"
                                required>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="company" class=" form-control-label">Длительность видео (в секундах)</label>
                            <input type="text" 
                                placeholder="Введите длительность в секундах" 
                                class="form-control" 
                                name="duration"
                                required>
                        </div>
                    </div>
                </div>
                    
                    <div class="form-group">
                        <label for="company" class=" form-control-label">Ссылка из YouTube</label>
                        <input type="text" 
                               placeholder="Линк видео на YouTube" 
                               class="form-control" 
                               name="videoid"
                               required>
                    </div>

            </div>
            
            <div class="card-footer">
                <button type="submit" class="btn btn-primary btn-sm">
                    <i class="fa fa-dot-circle-o"></i> Сохранить
                </button>
            </div>
        </form>
     </div>
</div>
 </div>
{% endblock %}