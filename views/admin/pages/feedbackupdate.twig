{% extends 'admin/main.twig' %}
{% block admincontent %} 

<div class="row">
    <div class="col-md-12">
        <div class="overview-wrap">
            <h2 class="title-1">Редактирование отзыва</h2>
            <a href="{{path_for('admin.feedback')}}">Назад к списку →</a>
        </div>
    </div>
</div>

<div class="row m-t-10">
    <div class="col-md-12">
        {% include 'include/flash.twig' %} 
    </div>
</div>

<div class="row m-t-10">
<div class="col-lg-12">
        <div class="card">
        
        <form action="{{path_for('admin.updatefeedback')}}" method="post">
            
            <div class="card-header">
                Форма <strong>редактирования</strong>
            </div>

            <div class="card-body card-block">


                <div class="form-group">
                    <label for="company" class=" form-control-label">Текст отзыва</label>
                    <textarea rows="4"
                                class="form-control" 
                                name="feedbacktext"
                                required>{{feedback.feedbacktext}}</textarea>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group ">
                            <label for="company" class=" form-control-label">Автор</label>
                            <input type="text" 
                                placeholder="Введите название" 
                                class="form-control"
                                name="feedbackauthor"
                                value="{{feedback.feedbackauthor}}"
                                required>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="company" class=" form-control-label">Описание (не обязательно)</label>
                            <input type="text" 
                                placeholder="Введите длительность в секундах" 
                                class="form-control" 
                                value="{{feedback.authortitle}}"
                                name="authortitle">
                        </div>
                    </div>
                </div>
                

            </div>
            <input type="hidden" name="feedbackid" value="{{feedback.feedbackid}}">
            <div class="card-footer">
                <button type="submit" class="btn btn-primary btn-sm">
                    <i class="fa fa-dot-circle-o"></i> Обновить
                </button>
            </div>
        </form>
     </div>
</div>
 </div>
{% endblock %}