{% extends 'admin/main.twig' %}
{% block admincontent %} 

<div class="row">
    <div class="col-md-12">
        <div class="overview-wrap">
            <h2 class="title-1">Отзывы </h2>
            <a href="{{path_for('admin.addfeedback')}}">
                <button class="btn btn-success item">
                    <i class="zmdi zmdi-plus-circle"></i> Добавить
                </button>
            </a>
        </div>
    </div>
</div>

<div class="row m-t-10">
    <div class="col-md-12">
        {% include 'include/flash.twig' %} 
    </div>
</div>

<div class="row m-t-10">
    
    <div class="col-lg-12">

        <div class="table-responsive table--no-card m-b-40">
            <table class="table table-borderless table-striped table-earning">
                <thead>
                    <tr>
                        <th>№</th>
                        <th>Текст</th>
                        <th>Автор</th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                {% for f in feedback %}
                <tr >
                    <td>{{f.feedbackid}}</td>
                    <td>{{f.feedbacktext}}</td>
                    <td class="text-center">{{f.feedbackauthor}}</td>
                    <td>
                    <div class="table-data-feature">
                        <a href="{{base_url()}}/admin/feedback/edit/{{f.feedbackid}}">
                            <button class="item">
                                <i class="zmdi zmdi-edit"></i>
                            </button>
                        </a>

                        <a href="#" data-href="{{base_url()}}/admin/feedback/delete/{{f.feedbackid}}" data-toggle="modal" data-target="#confirm-delete" class="delete-button">
                            <button class="item">
                                <i class="zmdi zmdi-delete"></i>
                            </button>
                        </a>
                    </div>
                    </td>
                </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}